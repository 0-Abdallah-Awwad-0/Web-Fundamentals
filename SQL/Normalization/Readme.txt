this readme is for my thoughts to be structured:
1- first form: one column may store multiple values, so it violates 1NF.
2-third form: No non-key column depends on another non-key column
Everything should depend only on the primary key.
interests is not truly dependent on the student itself.
Columns should depend on the key, the whole key, and nothing but the key